const fs=require("fs");const path=require("path");const{refreshPlayer}=require("./espn-player");const{findOverallRecord}=require("./team-record");const file=path.join(__dirname,"..","data","bets.json");function pct(b){return(Number(b.current)||0)/(Number(b.target)||1)*100}async function refreshRecord(b){const r=await fetch("https://site.api.espn.com/apis/v2/sports/football/nfl/standings");if(!r.ok)throw new Error(`Standings ${r.status}`);const record=findOverallRecord(await r.json(),b.subject);if(!record)throw new Error(`No overall record found for ${b.subject}`);b.current=record.wins;b.losses=record.losses;b.ties=record.ties;b.teamGamesPlayed=record.wins+record.losses+record.ties;b.lastSync=new Date().toISOString();if((b.status||"open")==="open"&&pct(b)>=100)b.status="hit"}async function main(){const bets=JSON.parse(fs.readFileSync(file,"utf8"));for(const b of bets)try{if(b.kind==="record")await refreshRecord(b);else if(b.kind==="parlay"){for(const l of b.legs||[]){l.sport=l.sport||"nfl";await refreshPlayer(l)}}else if(!["award","future"].includes(b.kind))await refreshPlayer(b)}catch(e){console.warn(b.id||b.desc,e.message)}fs.writeFileSync(file,JSON.stringify(bets,null,2)+"\n")};main();
